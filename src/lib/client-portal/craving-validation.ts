// Validation logic for craving service (stub, add more as needed)

export function isValidIntensity(intensity: number): boolean {
  return intensity >= 1 && intensity <= 10;
}

// Add more validation functions here as needed
